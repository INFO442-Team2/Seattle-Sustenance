class MealProgramsModel {
    constructor(data) {
        this.data = data;
    }

    // use the user filters to apply to the csv
    FilterResults ( userInputs ) {
        // code to modify csv data based on filter selection
        return filteredData;
    }

}
