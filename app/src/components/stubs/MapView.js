import React, { Component } from 'react'

class MapView extends Component {
    constructor(props) {
        super(props)
    }

    // displays Map based on state
    render() {
        // More info about Google Maps API handling needed
    }
}

export default MapView;